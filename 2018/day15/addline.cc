#include <cassert>
#include <cstdio>
#include <cmath>
#include <algorithm>

using namespace std;

int main(void)
{
    long long int x;
    while(~scanf("%lld", &x))
        printf("%lld\n\n", x);
    return 0;
}
